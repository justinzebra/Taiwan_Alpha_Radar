# Optional overrides for `docker compose up`.
# Copy to .env and fill in if you want real LLM-generated reports.

# AI provider: mock (default, no key needed) | openai | anthropic
AI_PROVIDER=mock
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
